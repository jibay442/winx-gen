/**
 * COUCHE LÈVRES.
 *
 * Pour ajouter un dessin :
 *   1. Ajoute un `if (variant === 'lips_XX')` avec le SVG ci-dessous
 *   2. Utilise fill="var(--lip-color)" pour la couleur des lèvres
 *   3. Enregistre la variante dans src/data/variants.js → tableau LIPS
 *
 * ViewBox du SVG parent : 0 0 400 700
 * Position conseillée : autour de y=163, centrées sur x=200
 */
export default function LipsLayer({ variant }) {
  // Ajoute tes variantes ici :
  // if (variant === 'lips_01') { return <g>...</g> }

  return null
}
