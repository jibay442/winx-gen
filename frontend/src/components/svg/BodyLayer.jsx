/**
 * COUCHE CORPS — silhouette de base (peau).
 * Rendue après les ailes, avant les vêtements.
 *
 * Pour ajouter un dessin :
 *   1. Ajoute un `if (variant === 'body_XX')` avec le SVG ci-dessous
 *   2. Utilise fill="var(--skin-color)" sur les zones de peau
 *   3. Enregistre la variante dans src/data/variants.js → tableau BODIES
 *
 * ViewBox du SVG parent : 0 0 400 700
 */
export default function BodyLayer({ variant }) {
  // Ajoute tes variantes ici :
  // if (variant === 'body_01') { return <g>...</g> }

  return null
}
