/**
 * COUCHE YEUX.
 *
 * Pour ajouter un dessin :
 *   1. Ajoute un `if (variant === 'eyes_XX')` avec le SVG ci-dessous
 *   2. Utilise fill="var(--eye-color)" pour la couleur de l'iris
 *   3. Enregistre la variante dans src/data/variants.js → tableau EYES
 *
 * ViewBox du SVG parent : 0 0 400 700
 * Position conseillée des yeux : autour de y=115, œil gauche cx≈168, œil droit cx≈232
 */
export default function EyesLayer({ variant }) {
  // Ajoute tes variantes ici :
  // if (variant === 'eyes_01') { return <g>...</g> }

  return null
}
