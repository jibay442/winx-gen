/**
 * COUCHE VÊTEMENTS — recouvre le corps.
 *
 * Pour ajouter un dessin :
 *   1. Ajoute un `if (variant === 'outfit_XX')` avec le SVG ci-dessous
 *   2. Utilise fill="var(--outfit-color)" pour la couleur principale
 *      et fill="var(--outfit-color2)" pour les accents/détails
 *   3. Enregistre la variante dans src/data/variants.js → tableau OUTFITS
 *
 * ViewBox du SVG parent : 0 0 400 700
 */
export default function OutfitLayer({ variant }) {
  // Ajoute tes variantes ici :
  // if (variant === 'outfit_01') { return <g>...</g> }

  return null
}
